# Rmount Configuration
# This file is automatically generated

# Path to rclone.exe (can be absolute or relative)
# Default: Searches in program directory first (portable), then PATH
# Examples:
#   rclone.exe                    (searches program dir, then PATH)
#   C:\Tools\rclone\rclone.exe    (absolute path)
#   .\rclone.exe                  (explicit relative to program dir)
Rclone=rclone.exe

# Path to rclone.conf configuration file
# Default: Searches multiple locations in order:
#   1. Program directory (portable)
#   2. C:\Users\<USERNAME>\.config\rclone\rclone.conf
#   3. %APPDATA%\rclone\rclone.conf
# Examples:
#   rclone.conf                           (program directory - portable)
#   C:\Users\JohnDoe\.config\rclone\rclone.conf
#   %APPDATA%\rclone\rclone.conf
Config=rclone.conf

# Enable autostart with Windows (true/false)
# When enabled, adds registry entry to start with Windows
# Registry path: HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
AutoStart=false
